services:
  - type: web
    name: phishshield
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app_phishing.py"
    pythonVersion: 3.12.3
    plan: free
    branch: main
    region: oregon
    
    
